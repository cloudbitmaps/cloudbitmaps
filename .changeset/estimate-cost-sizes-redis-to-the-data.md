---
"@cloudbitmaps/core": minor
---

`estimateCost()` compares with the Redis that would hold the data, sized from a catalogue of ElastiCache node
types, instead of one $346 cluster. The user-facing entry is in the root `CHANGELOG.md`, under `[Unreleased]`.
