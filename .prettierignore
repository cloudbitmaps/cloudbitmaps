dist
packages/*/dist
fuzz/build
coverage
node_modules
pnpm-lock.yaml

# Markdown is hand-formatted (ASCII diagrams, alignment) — Prettier would mangle it.
**/*.md

# site/ pages are matched pixel-for-pixel against the delivered design; Prettier reflows the <i> runs in the
# chunk-key grid and the <pre> code samples, which changes what renders.
site/*.html

# site-old/ is the superseded site, kept only to diff against until it is deleted.
site-old/
