dist
packages/*/dist
fuzz/build
coverage
node_modules
.worktrees
pnpm-lock.yaml

# Calibration evidence is the harness's own output, committed unedited. A formatter rewriting one — which the
# pre-commit hook would do without asking — would leave a file that is no longer the one the run wrote.
bench/calibration/*.json

# Markdown is hand-formatted (ASCII diagrams, alignment) — Prettier would mangle it.
**/*.md

# site/ pages are matched pixel-for-pixel against the delivered design; Prettier reflows the <i> runs in the
# chunk-key grid and the <pre> code samples, which changes what renders.
site/*.html

